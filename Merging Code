# Merging code

# Way 1
git diff <-branch name-> (to compare commits,branches,files & more)
git merge <-branch name-> (to merge 2 branches current and other)

# Way 2
Create a PR(Pull Request) 
# Pull Request
It lets you tell others about changes you've pushed to a branch in a repository on GitHub.
# Pull Command

git pull origin main

used to fetch and download content from a remote repo and immediately update the local repo to match that content.

# Resolving Merge Conflicts 

An event that takes place when Git is unable to automatically resolve differences in code between two commits.
When there's a difference in code in the same line this event occurs.
To resolve this conflict We can accept current change, accept incoming change or can accept both changes or can compare changes.